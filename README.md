# writing a binary search algorithim

## function name is_binary_search

### The function takes two int argument, slice of values of n and value of t and returns int as output
```go
func is_binary_search(n []int, t int)int{

}
```

### The is_binary_search function is called in the main function to print output

```go
func main(){
    
}
