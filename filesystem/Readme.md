# Filesystem in GO

### learning the Go file system API

### wrote a function that check for error and access a directory/folder and open the file in the folder and read the content of the file

- NB. this is a simple file system that only open and read content of a file in a folder